name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  biometric-id:
    name: BiometricID
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: BiometricID
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        env:
          DATABASE_URL: "sqlite://"
          VC_SIGNING_KEY: "ci-test-key"
          TEMPLATE_ENCRYPTION_KEY: "ci-test-key"
          API_KEY: ""
        run: |
          pytest tests/ -v --tb=short

  multi-blockchain:
    name: Multi-Blockchain
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Multi-Blockchain
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          pytest tests/ -v --tb=short
